/**
 * 
 */

/**
 * @author ETY8
 *
 */
public class notes {

}

//javadoc raccourci = ctrl + alt + j

//windows -> preferences -> javadoc -> code templates -> comments -> fields -> /** ${field} : ${field_type}  */

// Pour automatiser les getters / setters

/** Getter pour ${field}
* @return the ${bare_field_name}
*/

/** Setter pour ${field}
 * @return the ${bare_field_name}
 */